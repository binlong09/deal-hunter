# Deal Hunter PWA - System Architecture

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        USER DEVICES                          │
│  ┌──────────────┐           ┌──────────────┐                │
│  │   iPhone     │           │   Desktop    │                │
│  │   (Safari)   │           │   (Chrome)   │                │
│  └──────┬───────┘           └──────┬───────┘                │
│         │                          │                         │
│         └──────────┬───────────────┘                         │
│                    │                                         │
└────────────────────┼─────────────────────────────────────────┘
                     │
                     │ HTTPS
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                    VERCEL EDGE NETWORK                       │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              Next.js 15 App (PWA)                    │   │
│  │                                                      │   │
│  │  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐    │   │
│  │  │Capture │  │Dashboard│ │Generate│  │ Admin  │    │   │
│  │  │  Page  │  │  Page   │ │  Page  │  │  Page  │    │   │
│  │  └───┬────┘  └───┬────┘  └───┬────┘  └───┬────┘    │   │
│  │      │           │           │           │          │   │
│  │      └───────────┴───────────┴───────────┘          │   │
│  │                    │                                │   │
│  │                    ▼                                │   │
│  │  ┌──────────────────────────────────────────────┐  │   │
│  │  │           API Routes (Serverless)            │  │   │
│  │  │  /trips  /products  /generate  /examples    │  │   │
│  │  └──────┬───────┬──────────┬──────────┬────────┘  │   │
│  └─────────┼───────┼──────────┼──────────┼───────────┘   │
└────────────┼───────┼──────────┼──────────┼───────────────┘
             │       │          │          │
             │       │          │          │
    ┌────────┘       │          │          └──────────┐
    │                │          │                     │
    ▼                ▼          ▼                     ▼
┌─────────┐    ┌──────────┐  ┌──────────┐      ┌──────────┐
│  TURSO  │    │  VERCEL  │  │ANTHROPIC │      │  TURSO   │
│Database │    │   BLOB   │  │  CLAUDE  │      │ Database │
│(SQLite) │    │ Storage  │  │   API    │      │(Examples)│
│         │    │          │  │          │      │          │
│ trips   │    │ Images   │  │ Sonnet 4 │      │ example_ │
│products │    │Thumbnails│  │          │      │  posts   │
│gen_posts│    │          │  │          │      │          │
└─────────┘    └──────────┘  └──────────┘      └──────────┘
```

## 🔄 Data Flow Diagrams

### 1. Photo Capture Flow

```
┌─────────────┐
│   iPhone    │
│  (Husband)  │
└──────┬──────┘
       │ 1. Open /capture
       │ 2. Select category
       │ 3. Tap to capture
       ▼
┌─────────────────┐
│ CameraCapture   │
│   Component     │
│                 │
│ - Access camera │
│ - Capture photo │
│ - Create File   │
└────────┬────────┘
         │ 4. File blob
         │
         ▼
┌─────────────────┐
│  Upload Queue   │
│                 │
│ Background      │
│ processing      │
└────────┬────────┘
         │ 5. POST /api/products/upload
         │    FormData: image, tripId, category
         ▼
┌─────────────────┐
│  Upload API     │
│                 │
│ 1. Validate     │
│ 2. Upload Blob  │──────┐
│ 3. Save to DB   │──┐   │
└─────────────────┘  │   │
                     │   │
         ┌───────────┘   │
         │               │
         ▼               ▼
    ┌─────────┐    ┌──────────┐
    │  TURSO  │    │  VERCEL  │
    │Database │    │   BLOB   │
    │         │    │          │
    │INSERT   │    │ image.jpg│
    │product  │    │thumb.jpg │
    └─────────┘    └──────────┘
```

### 2. Product Curation Flow

```
┌─────────────┐
│   Desktop   │
│   (Wife)    │
└──────┬──────┘
       │ 1. Open /dashboard
       │
       ▼
┌─────────────────┐
│ Dashboard Page  │
│                 │
│ GET /api/       │
│   products      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Products API   │
│                 │
│ SELECT * FROM   │
│   products      │
│ WHERE status... │
└────────┬────────┘
         │
         ▼
    ┌─────────┐
    │  TURSO  │
    │Database │
    │         │
    │ Returns │
    │ Products│
    └────┬────┘
         │
         │ 2. Display grid
         ▼
┌─────────────────┐
│ Product Grid    │
│                 │
│ [📷] [📷] [📷]   │
│ [📷] [📷] [📷]   │
└────────┬────────┘
         │
         │ 3. Select & approve
         ▼
┌─────────────────┐
│ Bulk Update     │
│                 │
│ PATCH /api/     │
│   products/     │
│   bulk-update   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Bulk API       │
│                 │
│ UPDATE products │
│ SET status =    │
│   'approved'    │
│ WHERE id IN...  │
└────────┬────────┘
         │
         ▼
    ┌─────────┐
    │  TURSO  │
    │Database │
    │         │
    │ Updated │
    └─────────┘
```

### 3. AI Post Generation Flow

```
┌─────────────┐
│   Desktop   │
│   (Wife)    │
└──────┬──────┘
       │ 1. Open /generate
       │ 2. Select products
       │ 3. Click "Generate"
       │
       ▼
┌─────────────────┐
│ Generate Page   │
│                 │
│ POST /api/      │
│   generate-posts│
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  Generate Posts API                     │
│                                         │
│ 1. Fetch selected products              │
│ 2. Fetch example posts                  │
│ 3. For each product:                    │
│    - Generate post (Claude AI)          │
│    - Optimize image (Facebook)          │
│    - Schedule post                      │
│    - Save to database                   │
└───┬─────────┬──────────┬────────────────┘
    │         │          │
    │         │          │
    ▼         ▼          ▼
┌─────────┐ ┌──────────┐ ┌──────────┐
│  TURSO  │ │ANTHROPIC │ │  VERCEL  │
│Database │ │  CLAUDE  │ │   BLOB   │
│         │ │   API    │ │          │
│SELECT   │ │          │ │ Optimize │
│products │ │ Generate │ │  image   │
│examples │ │   text   │ │          │
└────┬────┘ └─────┬────┘ └─────┬────┘
     │            │            │
     │            │            │
     └────────────┴────────────┘
                  │
                  ▼
          ┌───────────────┐
          │ INSERT INTO   │
          │ generated_    │
          │   posts       │
          └───────┬───────┘
                  │
                  ▼
          ┌───────────────┐
          │  Return posts │
          │  with schedule│
          └───────┬───────┘
                  │
                  ▼
          ┌───────────────┐
          │ Display posts │
          │ Copy & Download│
          └───────────────┘
```

## 🧩 Component Architecture

### Frontend Components

```
┌──────────────────────────────────────────┐
│           Root Layout (layout.tsx)        │
│                                          │
│  - PWA Metadata                          │
│  - Global Styles                         │
│  - Theme Configuration                   │
└───────────────┬──────────────────────────┘
                │
    ┌───────────┴───────────┐
    │                       │
    ▼                       ▼
┌─────────────┐      ┌────────────────┐
│ Page Routes │      │   Components   │
└─────────────┘      └────────────────┘
    │                       │
    ├── Home               └── CameraCapture
    ├── Capture                 - Video stream
    ├── Dashboard               - Photo capture
    ├── Generate                - Upload queue
    └── Admin                   - Visual feedback
```

### Backend API Structure

```
┌──────────────────────────────────────────┐
│         API Routes (Serverless)          │
└──────────────┬───────────────────────────┘
               │
    ┌──────────┴──────────┐
    │                     │
    ▼                     ▼
┌─────────┐        ┌──────────────┐
│Resource │        │  Utilities   │
│ Routes  │        │              │
└─────────┘        └──────────────┘
    │                     │
    ├── /trips           ├── lib/turso.ts
    ├── /products        ├── lib/blob.ts
    ├── /generate-posts  ├── lib/claude.ts
    ├── /generated-posts └── lib/utils.ts
    └── /example-posts
```

## 💾 Database Schema

```
┌─────────────────────────────────────────────┐
│              TURSO DATABASE                  │
│           (SQLite Edge Replicas)             │
└──────────────┬──────────────────────────────┘
               │
    ┌──────────┴──────────┐
    │                     │
    ▼                     ▼
┌─────────┐        ┌──────────────┐
│ Trips   │        │   Products   │
│         │        │              │
│ id      │◄───────┤ trip_id      │
│ date    │        │ image_url    │
│ store   │        │ category     │
│ status  │        │ price        │
└─────────┘        │ status       │
                   └──────┬───────┘
                          │
              ┌───────────┴──────────┐
              │                      │
              ▼                      ▼
       ┌──────────────┐      ┌──────────────┐
       │ Generated    │      │  Example     │
       │   Posts      │      │   Posts      │
       │              │      │              │
       │ product_id   │      │ category     │
       │ post_text    │      │ post_text    │
       │ scheduled_*  │      │ is_active    │
       │ status       │      └──────────────┘
       └──────────────┘
```

## 🔐 Security Architecture

```
┌─────────────────────────────────────────┐
│          Security Layers                 │
└────────────┬────────────────────────────┘
             │
    ┌────────┴────────┐
    │                 │
    ▼                 ▼
┌─────────┐    ┌──────────────┐
│ Network │    │ Application  │
│ Security│    │   Security   │
└─────────┘    └──────────────┘
    │                 │
    ├── HTTPS Only    ├── Env Variables
    ├── Vercel Edge   ├── No API Keys in Code
    └── CDN           ├── Input Validation
                      └── SQL Injection Protection

    ┌──────────────────┐
    │ Data Security    │
    └──────────────────┘
           │
           ├── Turso Auth Tokens
           ├── Signed Blob URLs
           └── Scoped API Keys
```

## 📱 PWA Architecture

```
┌─────────────────────────────────────────┐
│          Progressive Web App             │
└────────────┬────────────────────────────┘
             │
    ┌────────┴────────┐
    │                 │
    ▼                 ▼
┌─────────┐    ┌──────────────┐
│Manifest │    │ App Features │
└─────────┘    └──────────────┘
    │                 │
    ├── Name          ├── Installable
    ├── Icons         ├── Offline-ready
    ├── Theme         ├── Native feel
    ├── Display       ├── Camera access
    └── Shortcuts     └── Responsive
```

## 🌐 Deployment Architecture

```
┌─────────────────────────────────────────────┐
│                VERCEL PLATFORM               │
└──────────────┬──────────────────────────────┘
               │
    ┌──────────┴──────────┐
    │                     │
    ▼                     ▼
┌──────────┐      ┌──────────────┐
│   Edge   │      │   Storage    │
│ Network  │      │              │
└──────────┘      └──────────────┘
    │                     │
    ├── Functions         ├── Blob Store
    ├── CDN               └── 1GB Free
    ├── SSL
    └── Auto-scale

    Global Replicas:
    ┌────┬────┬────┬────┐
    │ US │ EU │ASIA│APAC│
    └────┴────┴────┴────┘
```

## 🔄 State Management

```
┌─────────────────────────────────────────┐
│          State Architecture              │
└────────────┬────────────────────────────┘
             │
    ┌────────┴────────┐
    │                 │
    ▼                 ▼
┌─────────┐    ┌──────────────┐
│ Server  │    │   Client     │
│  State  │    │    State     │
└─────────┘    └──────────────┘
    │                 │
    ├── Turso DB      ├── React State
    │   (Source of    ├── useEffect
    │    truth)       ├── useState
    │                 └── Form State
    │
    └── Sync via API calls
```

## 📊 Performance Architecture

```
┌─────────────────────────────────────────┐
│       Performance Optimizations          │
└────────────┬────────────────────────────┘
             │
    ┌────────┴────────┐
    │                 │
    ▼                 ▼
┌─────────┐    ┌──────────────┐
│Frontend │    │   Backend    │
└─────────┘    └──────────────┘
    │                 │
    ├── Code Split    ├── Serverless
    ├── Lazy Load     ├── Edge Functions
    ├── Image Opt     ├── DB Replicas
    ├── CSS Purge     └── CDN Cache
    └── PWA Cache
```

## 🎯 Cost Architecture

```
Monthly Costs:
┌─────────────────────────────────────────┐
│  Vercel Free Tier:        $0/month      │
│  - 100GB bandwidth                      │
│  - 125k function calls                  │
│  - Unlimited deployments                │
├─────────────────────────────────────────┤
│  Turso Free Tier:         $0/month      │
│  - 500 databases                        │
│  - 1GB storage                          │
│  - Unlimited reads                      │
├─────────────────────────────────────────┤
│  Vercel Blob:             $0-5/month    │
│  - 1GB free                             │
│  - $0.15/GB after                       │
├─────────────────────────────────────────┤
│  Claude API:              $2-3/month    │
│  - Pay per use                          │
│  - ~$0.003 per post                     │
├─────────────────────────────────────────┤
│  TOTAL:                   $2-8/month    │
└─────────────────────────────────────────┘
```

## 🔄 Request Flow Example

### Complete User Journey

```
1. User opens app on iPhone
   │
   ├── DNS resolves to Vercel
   ├── SSL handshake
   ├── Load from nearest edge
   └── Render PWA

2. User navigates to /capture
   │
   ├── Client-side routing (instant)
   ├── Request camera permission
   └── Initialize video stream

3. User taps to capture photo
   │
   ├── Canvas draws frame
   ├── Convert to JPEG blob
   ├── Add to upload queue
   └── Visual feedback

4. Background upload
   │
   ├── POST to /api/products/upload
   ├── Serverless function runs
   ├── Upload to Vercel Blob (CDN)
   ├── Save URL to Turso
   └── Return success

5. User opens /dashboard
   │
   ├── GET /api/products
   ├── Query Turso (edge replica)
   ├── Return JSON
   └── Render grid

6. User generates posts
   │
   ├── Select products
   ├── POST /api/generate-posts
   ├── Fetch from Turso
   ├── Call Claude API
   ├── Optimize images
   ├── Save to Turso
   └── Return generated posts
```

## 🎨 Technology Stack Layers

```
┌─────────────────────────────────────────┐
│         Presentation Layer               │
│  React 19, Next.js 15, Tailwind CSS     │
└────────────┬────────────────────────────┘
             │
┌─────────────────────────────────────────┐
│          Application Layer               │
│    Next.js App Router, TypeScript       │
└────────────┬────────────────────────────┘
             │
┌─────────────────────────────────────────┐
│           Business Logic                 │
│   API Routes, Utilities, Components     │
└────────────┬────────────────────────────┘
             │
┌─────────────────────────────────────────┐
│           Data Layer                     │
│   Turso, Vercel Blob, Claude API        │
└────────────┬────────────────────────────┘
             │
┌─────────────────────────────────────────┐
│        Infrastructure Layer              │
│     Vercel Edge Network, CDN            │
└─────────────────────────────────────────┘
```

## 📈 Scalability

Current implementation supports:
- **Users**: 2 (Husband + Wife)
- **Products**: Unlimited (DB: 1GB free)
- **Images**: ~5,000 photos (1GB Blob)
- **Posts**: ~1,000/month (Claude API)
- **Traffic**: 100GB/month (Vercel)

To scale beyond:
- **Vercel Pro**: $20/month → 1TB bandwidth
- **Turso Scaler**: $29/month → Unlimited DBs
- **More Blob**: $0.15/GB → Add storage as needed
- **Claude API**: Pay-as-you-go → Automatic scaling

---

This architecture is designed for:
✅ **Low cost** (~$2-3/month)
✅ **High performance** (Edge network)
✅ **Easy maintenance** (Serverless)
✅ **Future-proof** (Can scale easily)
